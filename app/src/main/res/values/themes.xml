<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MemoWithTags" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- 상태바 투명화 -->
        <item name="android:windowTranslucentStatus">true</item> <!-- 반투명 효과 -->
    </style>

    <style name="Theme.MemoWithTags" parent="Base.Theme.MemoWithTags" />
</resources>